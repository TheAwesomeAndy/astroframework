import {createModelIdentity,createResearchStatus} from './research-regime-registry.mjs';
export const DISCOVERY_CANDIDATE_MODEL='naf.research.discovery_candidate_registry.v0.4.5';
function hashText(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)>>>0}return h.toString(16).padStart(8,'0').toUpperCase();}
function canonical(f){return JSON.stringify({kind:f?.kind||'unknown',participants:[...(f?.participants||[])].sort(),measurement:f?.measurement||null});}
export function candidateFromFinding(f){const id=`NAF-CANDIDATE-${hashText(canonical(f))}`;return {candidate_id:id,source_finding_id:f?.id||null,temporary_label:f?.kind||'unnamed_structure',description:f?.statement||'',participants:[...(f?.participants||[])],measurement:f?.measurement||null,derivation_ref:f?.derivation_ref||null,model_identity:createModelIdentity({regime:'discovery',model_id:`${DISCOVERY_CANDIDATE_MODEL}.${id}`,status:createResearchStatus({geometry:'verified',derivation:f?.derivation_ref?'verified':'unknown',null_comparison:'pending',population_frequency:'unknown',replication:'none',interpretation:'withheld'})}),promotion_state:'detected-no-baseline'};}
export function buildDiscoveryCandidateRegistry(discoveries){const candidates=(discoveries?.findings||[]).map(candidateFromFinding);return {model_id:DISCOVERY_CANDIDATE_MODEL,version:'0.4.5',regime:'discovery',candidates,count:candidates.length,promotion_gate:{required:['verified derivation','null comparison','population baseline','replication','separate provisional interpretation'],rule:'Detection alone does not promote a candidate into the Operational model.'}};}
